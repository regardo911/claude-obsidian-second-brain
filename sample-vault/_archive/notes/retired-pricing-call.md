# Retired: usage-based pricing for exports

Killed 2026-02-11. Usage-based billing on an endpoint that fails on wide date
ranges would have billed the client for our own retries. Revisit only after
the join fix ships and the failure rate is measured, not estimated.

This note is archived on purpose. It is still linked from the active vault,
which is exactly the case a link checker must not report as dead.
