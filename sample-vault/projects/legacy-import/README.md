# legacy-import

One-off CSV importer for the same client. Paused: they have not frozen the
source schema, so every mapping I write goes stale within a week.

Resume when they confirm the column list. Scope as quoted lives in
[[zenith-scope-v2]].
